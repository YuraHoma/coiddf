---
layout: base.njk
permalink: /dopomogty/
title: Отримати допомогу | Фонд розвитку міжнародної співпраці
description: Як звернутися по допомогу до фонду та хто може її отримати.
---
      <section class="page-hero section-dark">
        <div>
          <p class="eyebrow">{{ dopomogty.hero.eyebrow }}</p>
          <h1>{{ dopomogty.hero.heading }}</h1>
          <p class="lead">{{ dopomogty.hero.lead }}</p>
        </div>
      </section>
      <section class="section">
        <div class="info-grid">
          {%- for c in dopomogty.cards %}
          <article><h3>{{ c.title }}</h3><p>{{ c.text | safe }}</p></article>
          {%- endfor %}
        </div>
      </section>
