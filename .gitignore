node_modules/
_site/
.wrangler/
.DS_Store
*.bak*

# Робочий файл перекладача — генерується з catalog.json на вимогу
# (npm run i18n:export) і повертається через npm run i18n:import.
# Джерело правди — catalog.json, він у git.
scripts/i18n/translations.csv

# Локальні секрети для `wrangler dev` — у git їм не місце.
.dev.vars

# local agent workspaces
.claude/
